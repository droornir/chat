# Chat App
    this app isn't finished yet:
    most of the modules are incomplete,
    server (recieving API calls via Express) started but not done,
    tests are not written but the infra for them is set
    
  - How to run:
    on app folder,
    run "npm install",
    run "ng serve"

